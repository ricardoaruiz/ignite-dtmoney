import styled from 'styled-components';

import * as SummaryStyle from 'components/Summary/styles';

export const Wrapper = styled.main`
  ${SummaryStyle.Wrapper} {
    position: relative;
    bottom: 7rem;
  }
`;
