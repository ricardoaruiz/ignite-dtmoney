export interface Theme {
  colors: {
    red: string;
    green: string;
    blue: string;
    blueLight: string;
    textTitle: string;
    textBody: string;
    inputBackground: string;
    inputBorder: string;
    background: string;
    shape: string;
  };
}
